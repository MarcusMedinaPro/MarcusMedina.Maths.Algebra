# CI Workflow Template
# Använd för NuGet-paket med .NET 10+
# Säkerhetsscanningar hanteras av separata workflows:
#   security-code-scan.yml — C# SAST (SecurityCodeScan)
#   osv-scanner.yml        — dependency vulnerabilities (Google OSV)

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

env:
  DOTNET_VERSION: '10.0.x'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore
        working-directory: ./csharp

      - name: Build
        run: dotnet build --configuration Release --no-restore /p:TreatWarningsAsErrors=true
        working-directory: ./csharp

      - name: Test
        run: |
          dotnet test \
            --configuration Release \
            --no-build \
            --logger "trx;LogFileName=test-results.trx" \
            --collect:"XPlat Code Coverage"
        working-directory: ./csharp

      - name: Upload Test Results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-results
          path: ./csharp/**/TestResults/**/*.trx

  test-matrix:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: build-and-test

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore & Test
        run: dotnet test --configuration Release
        working-directory: ./csharp

  pack:
    name: Build NuGet Package
    runs-on: ubuntu-latest
    needs: [build-and-test, test-matrix]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Pack
        run: dotnet pack --configuration Release --no-build --output ./artifacts
        working-directory: ./csharp

      - name: Upload Package
        uses: actions/upload-artifact@v7
        with:
          name: nuget-package
          path: ./csharp/artifacts/*.nupkg
